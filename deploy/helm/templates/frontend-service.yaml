apiVersion: v1
kind: Service
metadata:
  name: "{{ .Release.Name }}-frontend"
spec:
  selector:
    app: perplexica-frontend
  ports:
    - protocol: TCP
      port: {{ .Values.perplexica.frontend.service.port }}
      targetPort: {{ .Values.perplexica.frontend.service.port }}
